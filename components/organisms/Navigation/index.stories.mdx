import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Navigation from './index.vue';
import { withA11y } from '@storybook/addon-a11y';

<Meta
  title="Organisms/Navigation"
  component={Navigation}
  decorator={withA11y}
  argTypes={{
    list: {
      description: 'a set of items consisting of path and label for navigation',
      control: {
        type: 'object'
      }
    }
  }}
/>


export const NavigationTemplate = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: {Navigation},
  template: `
    <Navigation
      :list="list"
      :heading-content="headingContent"
      :is-heading-visually-hidden="isHeadingVisuallyHidden"
    />
  `
});

# Navigation


<Canvas>
  <Story
    name="Navigation"
    args = {{
      list: [
        {path: `path1`, label: `링크1`}, {path: `path2`, label: `링크2`}
      ],
      headingContent: '메인 내비게이션',
      isHeadingVisuallyHidden: false
    }}
  >
    { NavigationTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="Navigation" />