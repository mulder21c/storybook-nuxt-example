import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import MainTemplate from './index.vue';
import { withA11y } from '@storybook/addon-a11y';

<Meta
  title="Templates/Main"
  component={MainTemplate}
  decorator={withA11y}
  argTypes={{
    list: {
      description: 'a set of items consisting of path and label for navigation',
      control: {
        type: 'object'
      }
    },
  }}
/>


export const MainPageTemplate = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: {MainTemplate},
  template: `
    <MainTemplate
      :list="list"
      :heading-content="headingContent"
      :visually-hidden-heading="visuallyHiddenHeading" />
  `
});

# Navigation


<Canvas>
  <Story
    name="MainTemplate"
    args = {{
      list: [
        {path: `path1`, label: `링크1`}, {path: `path2`, label: `링크2`}, {path: `path3`, label: `링크3`}
      ],
      headingContent: `메인 내비게이션`,
      visuallyHiddenHeading: false,
    }}
  >
    { MainPageTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="MainTemplate" />