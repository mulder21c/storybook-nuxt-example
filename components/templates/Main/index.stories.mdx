import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import MainTemplate from './index.vue';

<Meta
  title="Templates/Main"
  component={MainTemplate}
/>

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: {MainTemplate},
  template: `
    <MainTemplate
      :list="list"
      :heading-content="headingContent"
      :is-visually-hidden-heading="isVisuallyHiddenHeading"
    />
  `
});

# Main Page Template


<Canvas>
  <Story
    name="MainTemplate"
    args = {{
      list: [
        {path: `path1`, label: `링크1`}, {path: `path2`, label: `링크2`}, {path: `path3`, label: `링크3`}
      ],
      headingContent: `메인 내비게이션`,
      isVisuallyHiddenHeading: false,
    }}
  >
    { Template.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="MainTemplate" />