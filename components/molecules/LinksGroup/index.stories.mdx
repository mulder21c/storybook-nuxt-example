
import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import LinksGroup from './index.vue';
import { action } from '@storybook/addon-actions';
import { withA11y } from '@storybook/addon-a11y';

<Meta
  title="Molecules/Links"
  component={LinksGroup}
  decorator={withA11y}
  argTypes={{
    linkList: {
      description: 'a set of items consisting of path and label',
      control: {
        type: `object`
      }
    },
    wrapper: {
      description: 'wrapper HTML element',
      control: {
        type: `select`,
        options: ['div', 'nav']
      }
    }
  }}
/>


export const LinksGroupTemplate = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: {LinksGroup},
  template: `
    <links-group :link-list="linkList" :wrapper="wrapper" />
  `
});

# Liks Group

Example for using `<nuxt-link>` in storybook

<Canvas>
  <Story
    name="LinksGroup"
    args = {{
      linkList: [
        {path: `path1`, label: `링크1`}, {path: `path2`, label: `링크2`}
      ],
      wrapper: 'div'
    }}
  >
    { LinksGroupTemplate.bind({}) }
  </Story>
</Canvas>

<ArgsTable story="LinksGroup" />